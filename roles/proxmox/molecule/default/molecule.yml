---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: xhiroga-ansible-role-proxmox-molecule
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
